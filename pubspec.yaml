name: clean_getx
description: " A CLI tool for scaffolding GetX features with clean architecture."
version: 0.0.1
publish_to: 'none'
homepage:

environment:
  sdk: ^3.12.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  args:
  path:
  yaml: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0


